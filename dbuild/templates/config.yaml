# dbuild configuration file
# Place in .daemonless/config.yaml or .dbuild.yaml (higher priority)
#
# Full reference: https://github.com/daemonless/dbuild

build:
  variants:
{%- if "latest" in variants %}
    - tag: latest
      containerfile: Containerfile
{%- endif %}
{%- if "pkg" in variants %}
    - tag: pkg
      containerfile: Containerfile.pkg
{%- endif %}
{%- if "pkg-latest" in variants %}
    - tag: pkg-latest
      containerfile: Containerfile.pkg
      args:
        BASE_VERSION: "15-latest"
{%- endif %}

cit:
  mode: health
  port: {{ port }}
  health: /
  {%- if mlock %}
  annotations:
    - "org.freebsd.jail.allow.mlock=true"
  {%- endif %}
